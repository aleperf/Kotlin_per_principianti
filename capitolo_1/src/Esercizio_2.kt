
/* TODO: scrivete una funzione main e all'interno di essa usate la funzione print() per scrivere
   la frase "Kotlin è un moderno linguaggio di programmazione".
   Sotto questa prima frase usate nuovamente la funzione print() per scrivere
   invece "C'è spazio per tutti nel mondo della programmazione"
   Infine guardate bene l'output nella console: c'è spazio per tutti,
   ma non c'è spazio fra queste 2 frasi!
   print() serve per stampare stringhe senza ritorno a capo, quindi quando si usa
   print() due volte di seguito le frasi vengono affiancate l'una all'altra.
   Se volete uno spazio dovete inserirlo voi.
   println() invece include sempre un ritorno a capo, quindi usando due volte di seguito
   println(), le frasi saranno ognuna su una propria riga.
 */